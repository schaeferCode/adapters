import { defineConfig } from 'vite';

export default defineConfig({
  // test: {
  //   globals: true,
  //   environment: 'node',  // Or 'jsdom' if testing frontend code
  //   include: ['packages/**/*.test.ts'],
  // },
});
