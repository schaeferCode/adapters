{
  "engines": {
    "node": ">=22.12.0"
  },
  "name": "@schaefercode/aws-dynamo-db-adapter",
  "version": "2.0.0",
  "main": "build/index.js",
  "files": [
    "build",
    "README.md"
  ],
  "types": "build/types/index.d.ts",
  "scripts": {
    "clean": "rm -rf ./build",
    "test": "vitest",
    "build": "npm run clean && tsc --build",
    "publish": "npm publish --access public"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "Custom dynamo db adapter",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.741.0"
  },
  "devDependencies": {
  }
}
